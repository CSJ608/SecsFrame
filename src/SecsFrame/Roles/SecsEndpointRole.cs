namespace SecsFrame;

/// <summary>Identifies the SECS application role of an endpoint.</summary>
public enum SecsEndpointRole
{
    /// <summary>The endpoint represents a factory host.</summary>
    Host,

    /// <summary>The endpoint represents manufacturing equipment.</summary>
    Equipment,
}
